# 预置 worker：OpenCode task 模式（spawn-cli 协议）
# {{prompt}} 由编排层以参数数组整体替换（prompt 全文单参数）；工作目录由编排层以 spawn cwd 保证=worktree
id: opencode
name: OpenCode
protocol: spawn-cli
capabilities: [task]
command: opencode run --standalone --format json {{prompt}}
timeoutMin: 30
